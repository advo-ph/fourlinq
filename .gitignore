# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.*
.vercel

# Playwright persistent browser profile for the Gemini image-gen script
# (contains Google session cookies — never commit)
.gemini-browser-state/
.visual-qa/
.prod-surface-audit/
tsconfig.*.tsbuildinfo

# Built server bundle (pm2 cluster entry) — produced by npm run build:server
server/index.bundle.cjs

# Generated WebP thumbnails — CI prebuild (npm run projects:thumbs) regenerates these
# Do not commit; 335 files would bloat the repo unnecessarily
public/images/projects-fb/thumbs/

# gpt-image script outputs
scripts/gpt-image-out/

# Delivery copy of the catalog PDF — byte-identical to the tracked copy under
# public/docs, written by npx tsx scripts/generate-system-catalog.ts
output/
.qa-3d*/
.qa-look/
.qa-film*/
.handoff-new/
.claude/chrome-devtools/screenshots/
