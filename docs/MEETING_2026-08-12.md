# Meeting sheet — 2026-08-12, 10:30

**Internal prep for the FourlinQ / ADVO team. Not a client message.** Nothing here is
drafted to be forwarded or read out verbatim as a reply. It is what we need in our
heads before the call.

**How to use it.** Section 1 walks her nine items in her order. Anything marked
**uncertain** should be said as uncertain out loud — a confident wrong line in this
meeting is worse than "we'll check that and come back to you."

**Evidence basis.** Everything below is checked against the working tree on
`main` (commit `1cc861c`) plus uncommitted work in the tree, on 2026-08-11.
The 3D numbers come from a real-browser run of `npm run qa:3d` on 2026-08-11:
**22 systems checked, 0 failing.** Where a claim could not be verified, it says so.

---

## 1. Her nine items from 2026-08-07

| # | Her words | Where it stands |
|---|---|---|
| 1 | Colors change the door too | **Done** |
| 2 | Products — Glass Railing | **Page is live. Photo is the gap.** |
| 3 | Products — SC-Door System | **Page is live. Photo is the gap.** |
| 4 | Casement door outward opening | **Done** |
| 5 | Photos by area | **Built, but empty for six of the seven areas** |
| 6 | Fixed-Slide-Slide-Slide-Slide-Fixed door | **Now a product page too. Still not in the 3D view.** |
| 7 | Automated windows | **Page is live, now with an approved image.** |
| 8 | Slim doors photos | **Now a product page with her approved image.** |
| 9 | Louvre windows | **Done in 3D. Photo still wanted.** |

---

### 1 — "when they click the colors, the door color will also change"

**Done.** Pick a finish, the door changes — including from the finish step in the
left-hand panel, which is the path a customer actually uses.

Proven in a real browser rather than read off the source. Clicking **Jet Black** in
the configurator's own finish list repaints the model (16% of the canvas changes)
and the viewer's label follows to "LIVE 3D · CASEMENT · JET BLACK". Golden Oak and
White behave the same. All six doors respond — `casement-door`, `french-door`,
`sliding-door`, `lift-slide`, `multislide`, `ninety-series` — and the flat drawing
mode always did.

> **A correction worth recording.** An earlier pass through this repo concluded from
> reading the code that the left-hand panel did *not* reach the model, and this sheet
> said so. That was wrong, and it would have talked down working software in front of
> the client. The code genuinely does look broken — the viewer reads the incoming
> finish once and never listens again — but the component remounts when the finish
> changes, so the value does get through. It has since been made explicit rather than
> left to reconciliation, because a demo is a bad place to find out a parent stopped
> remounting.

**Still worth asking her which page she meant.** On **Products** the colour chips are
display-only by design (`src/pages/Products.tsx:138-183`); nothing changes when you
click them there. If that is what she saw, the answer is "that page is a catalogue,
the Design Tool is where colour is live" — a different conversation from this one.

### 2 — "Products — Glass Railing"

**The page exists and is live** (`src/data/products.ts:290-308`) with description and
specs, one of which is already flagged as indicative only.

**What is missing is a photograph.** The tile is still a line drawing. The shot we need
is specified in `docs/AUG07_ASSET_REQUEST.md`: straight-on balcony or terrace railing,
frameless or minimal post, daylight, no scaffolding, ideally a FourlinQ install.

There is no 3D model for it and none is planned. Say this plainly if it comes up: a
glass railing is the only item on her list that is neither a window nor a door, so
putting it in the 3D viewer would be a bigger decision than it looks.

### 3 — "Products — SC-Door System — Sliding Casement Door"

**The page exists and is live** (`src/data/products.ts:310-328`). The description
deliberately distinguishes it from a swing leaf.

**Missing:** a photograph — the leaf mid-travel on its track, full leaf visible, clearly
not a swing casement and not an ordinary two-panel slider. No 3D model; the sliding-door
and casement-door models we have are different products and we will not substitute one
for the other.

**Ask her to confirm the name.** The page currently states the expansion "Sliding
Casement Door" as fact. That came from us reading her shorthand. If SC means something
else in her catalogue, we should correct it now rather than after it is printed.

### 4 — "Casement door should be outward opening"

**Done.** Outward is the default in the model builder, the exported 3D file swings a
full 90° outward, and the flat drawing carries an outward cue with the line "Casement
door swings outward — never inward."
(`scripts/handoff/model/swing-door-model.js:173`, `src/components/configurator/WindowPreview.tsx:732-733`.)

Two things she may reasonably ask that we should not overstate:

- **No words say it.** The product copy for the casement *door* is silent on direction —
  the drawing shows it, the text does not. The "opens outward" copy that exists is about
  casement and awning **windows**. If she wants it stated in writing, that copy has to
  be written.
- **No automated test guards the direction.** If the model is ever rebuilt with the wrong
  setting, nothing would catch it. The flat drawing side *is* guarded. Worth fixing;
  not worth raising unless she asks.

### 5 — "can we fix the photos - by area"

**The feature is built exactly to her convention. The data behind it is not there yet.**

Built and working: all seven areas she named are defined, the label format is her
example format — village, em dash, city ("Amara — Cebu") — and the Inspiration page
groups and filters by area (`src/data/project-area.ts:44-60`, `src/pages/Inspiration.tsx:328-482`).

The problem she will see immediately: **six of the seven areas are empty.** Of 61
projects, only Metro Manila (3) and Cebu (16) are tagged to an area. Davao, Zamboanga,
Butuan, Cagayan de Oro, Tacloban and Ilocos have no projects assigned, so those sections
do not appear at all. The honest line is: *the grouping works, we have no project records
tagged to those six areas yet* — not that the feature is broken.

And her own example does not yet render: `cebu-c-residence-amara` currently carries the
city "Liloan" with no village and no region, so it shows as "Liloan" and lands outside
the Cebu group. We deliberately did not guess "Amara" from the file name.

Her 2026-08-08 note — *"please refrain from indicating exact address in the website"* —
is **already satisfied**. The project data holds city or municipality only; no street
addresses are stored or shown.

> **Superseded later on 2026-08-12 — now resolved.** She followed up asking that projects
> be *renamed* to the area label rather than carrying it as a second line, and changed the
> convention recorded above in two ways:
>
> 1. **Separator is a comma, not an em dash** — `Amara, Cebu`. Her reason was how it looks
>    on the cards. The em dash is gone from the code and a test now guards against it
>    coming back.
> 2. **Villages released.** Asking for "amara - cebu" authorized the subdivision names
>    already sitting in the project slugs — the ones we had declined to guess at. Fourteen
>    projects now carry a village: Monterrazas, Maria Luisa, Kishanta, Vista Grande,
>    Molave, Amara, Nuvali, San Lorenzo.
>
> The second half of the label pairs a village with the **city in Metro Manila** and the
> **region everywhere else**. Her examples were "san lorenzo - makati" and "amara - cebu",
> and Amara is in Liloan — so Cebu, the region, is what she names those projects by.
>
> The privacy note above still holds. Slug fragments that read as client surnames
> (`fortunado`, `maratas`) were deliberately **not** promoted, and no address was added.

### 6 — "kindly include Fixed-Slide-Slide-Slide-Slide-Fixed Door in the design"

**In the design tool's flat drawing: yes, shipped** (`src/data/configurator.ts`).

**As a catalog product: yes, shipped 2026-08-12** — `fixed-slide-door` under doors,
on her approved image (`src/data/products.ts`, migration `023`). Until now the layout
existed only inside the design tool, so someone browsing `/products?filter=doors` could
not find it at all. The card states no opening width, for the reason below.

**In the 3D view: not yet.** The six-panel geometry has now been written — two fixed
leaves at the ends, the four middle leaves parting to both sides — but it has **not been
exported into the 3D viewer**, so it is not something anyone can click today, and the
2026-08-11 browser run still counts 22 systems, not 23. The remaining step is owned by
another person on the team.

**Do not say this is in the 3D viewer.** It is not, as of this sheet.

One open decision inside it: how wide a six-leaf bi-parting run we are willing to draw.
The working default is a 9 m opening, chosen so each leaf stays a normal size. **That is
our engineering choice, not a confirmed product spec** — and a 9 m opening drawn on the
site is the site saying FourlinQ can supply one. Someone with product authority has to
confirm that before it goes live. **Uncertain; needs an internal answer, not hers.**

### 7 — "automated windows"

**Two pages are live, not one.** Her earlier remarks covered two different things and we
split them: **Automated Windows** under windows (motorised openers for high or
hard-to-reach sashes) and **Automated Door Access** under doors (keypad / card / fob /
app entry). Both carry "indicative only, by project consultation" on the hardware specs.
(`src/data/products.ts:360-377`, `:385-403`.)

**Both now carry her approved images** (migration `023`), replacing the schematic line
drawings. These are white-background **renders, not photographs** — she approved these
specific ones on 2026-08-12. A real hardware shot is still the better asset: for windows,
a close detail of a chain actuator on a sash; for doors, the entry hardware on a real
leaf. No 3D for either.

### 8 — "slim doors photos..you need to add"

**This has moved further than any other item since the sheet was first written.**

It was the least far along of the nine: no product page, no photograph, and an open
question about what a slim door even is in her catalogue. We deliberately declined to
guess a mechanism and draw it.

**She supplied the image on 2026-08-12, and it answers the question** — a hinged leaf on
a narrow frame. So it **swings**; it does not slide. The product now exists as `slim-door`
under doors (`src/data/products.ts`, migration `023`).

Two things to keep straight in the room: the image is a **render, not a photograph**, and
the five-shot list in `docs/AUG07_PROJECT_AREA_REQUEST.md` (exterior closed, interior
closed, open, threshold detail, multi-panel run) is still worth asking for. The render
shows reeded glass, which is **not** a FourlinQ glass option — the copy deliberately says
"clear or privacy glass" instead, and does not sell what the picture shows.

### 9 — "louvre windows too"

**Done, and it is the one item that got materially better this week.** Louvre is now real
3D built from geometry we own: blades tilt together through 172°, in two variants
(18 narrow blades and 9 wide). It is in the viewer and it passed the 2026-08-11 browser
run (`louvre`, `louvre-wide`, both ok).

The product tile now shows a picture of that model rather than a line drawing. **It is a
render, not a photograph** — and she has previously pushed back on white-background
renders sitting next to real photos, so we treat this as a better placeholder, not as the
delivery. **The photo request for louvre still stands.**

---

## Blocked on her — two answers we need

**A. Tilt & Turn — does FourlinQ actually sell it?**
This is not on her nine-item list, but it collides with her own instruction from the
earlier meeting: *"Everything is going out. Never inward."* A tilt & turn genuinely opens
**inward** — that is what the hardware does, so it cannot be fixed by rewording anything.
Today it is listed as a FourlinQ offering in the glossary and as a type in the design tool,
and it is the only type with no 3D, on purpose. **If the answer is no, we delete the entry
and the type. If the answer is yes, "never inward" gains a documented exception.** We are
not building anything for it until she answers.

**B. The by-area table.**
`docs/AUG07_PROJECT_AREA_REQUEST.md` is a per-project table waiting on her: village /
subdivision, city, province, region. Two specific things to get from her in the room:

1. Confirm **Amara → Cebu** so her own worked example renders the way she asked.
2. Her example said **"Anvaya Cove, Batangas."** Anvaya Cove is in **Morong, Bataan**.
   We have not quietly corrected it and will not. Ask which she meant.

Also worth setting expectations: filling this table is what makes the six empty areas
appear. Until rows exist for Davao, Zamboanga, Butuan, CDO, Tacloban and Ilocos, those
sections stay absent no matter what we build.

---

## 2. What changed since she last looked

Said in her terms, not ours:

- **The 3D view now covers 22 window and door systems**, all built from geometry we own,
  and it is checked in a real browser rather than by eye. Of those 22, **18 have an
  open/close control and all 18 move when clicked**; the other four are fixed panes and a
  corner combination that have nothing to open. (Bay and bow currently show their side
  panes as fixed even though they open in reality — a known, small gap.)
- **Choosing a finish now repaints doors, not just windows** — with the left-panel caveat
  in item 1 above. Do not claim this is complete end-to-end.
- **Louvre is real 3D now**, in two blade variants, and it opens.
- The site no longer renders any third-party licensed model. *(Internal housekeeping —
  only mention it if she asks why the louvre changed.)*

Do not describe anything else as "new" in this meeting. The rest of the list is either
unchanged or covered above.

---

## 3. The four product photos still outstanding

| Product | Shot needed | Status |
|---|---|---|
| Glass Railing | Straight-on balcony/terrace railing, frameless or minimal post, daylight, no scaffolding | Her approved render on the tile |
| SC-Door | Leaf mid-travel on its track, full leaf visible | **Line drawing on the tile — the last one** |
| Automated Windows | Chain actuator on a sash, or full unit with actuator visible | Her approved render on the tile |
| Automated Door Access | Keypad / card / fob entry hardware on a real leaf | Her approved render on the tile |

As of 2026-08-12 only **SC-Door** is still on a line drawing. Every other tile carries an
image she approved. That is a real improvement, and it does not close this section: an
approved render is not a photograph, and the standing ask for real project photography
(ROADMAP item 20) is unchanged. Louvre and the two automation tiles in particular would
all still be better as real shots.

**The decision the team has to make: client photos, or AI-generated images?**

- **Client photos** are what the tiles are built for and what she has previously insisted
  on. She rejected white-background synthetic renders before.
- **AI generation** is possible and we have a written method for it, but the standing rule
  is that accuracy, not provenance, is the bar: an AI image may only show a product
  FourlinQ genuinely sells, in a generic setting, never a fabricated FourlinQ project,
  client name, or address.

**Readiness verdict from the audit: not ready to generate these four.** Three of the four
are exactly the products whose mechanism or hardware we have not had confirmed — an AI
image of an SC-Door or a chain actuator would be us inventing what the product looks like,
which is the failure mode the rule exists to prevent. Glass railing is the one where an AI
image would be lowest-risk, since a frameless glass railing is generic. Recommended line
in the meeting: **ask her for real shots of all four first**, and treat AI as a fallback we
would bring back to her for approval image by image — not as a decision we make on our own.

---

## 4. The second project — attendance / accomplishment reports

Her four questions of 2026-08-07 are answered in full in
**`docs/ATTENDANCE_APP_QUESTIONS.md`**. Read it before the call; do not summarise it from
memory in the room.

Two framing points to carry in:

- It is a **separate project from the website**, with its own scope. There is no attendance
  code in this repo and never has been — **there is nothing to demo.**
- The one answer worth having ready is the shared-phone question, because it decides the
  shape of the whole thing. The document's position is to confirm identity rather than
  guess it (the person says who they are, the face confirms it), which is what makes a
  shared device workable at all.

---

## 5. Two things to RAISE — not report

These are ours to bring up. Neither is on her list, and both look worse the longer they go
unsaid.

**A. The `sales@fourlinq.com` credential has blocked lead email since May.**
The code that emails new website inquiries has been finished and wired into every inquiry
form since 2026-05-29. It has done nothing every day since, because the mailbox credential
was never supplied — the server still logs that the credentials are missing
(`server/lib/mailer.ts:42`). Zero development work remains; it goes live within minutes of
the credential landing. What is needed is one of: a Gmail app password for
`sales@fourlinq.com`, or a Resend API key.

Say the consequence plainly: **for that period, nobody was emailed when a lead came in.**
The inquiries were still recorded and are still in the admin view — nothing was lost — but
no one was notified. Do not let this be discovered later.

**B. Per-rep lead visibility was promised on 2026-05-31 and is not built.**
She was told in writing that a sales rep would log in and see only the leads assigned to
them, and that it was in progress. Where it actually stands:

- **Per-rep login: partly there.** Individual accounts, secure passwords and a "Sales
  Agent" role all exist.
- **The scoping does not exist.** There is no way to assign a lead to a rep anywhere in the
  system, so "only their own leads" cannot currently be expressed. Any rep logging in today
  sees the same list an admin sees.

This one has no blocker and no owner — it was simply never scheduled. Bring it up as ours
to own. **Do not commit a date in the room**; take the scope back and cost it properly.

---

## Do not say in this meeting

- Do **not** say the six-panel FSSSSF door is in the 3D viewer. It has a product page now; the 3D view is still pending.
- Do **not** say the finish picker is fully wired in 3D. The left panel is not.
- Do **not** call any product tile a photograph. Six of them are renders she approved; louvre is a render of our own model.
- Do **not** quote an opening width for the FSSSSF run. The 9 m default is our engineering choice, unconfirmed as product spec.
- Do **not** say the slim door comes in reeded glass. The render shows it; the catalogue does not carry it.
- Do **not** offer dates for anything on this sheet.
